#include <stdio.h>

int main() {
    
    int n, Cube;
    scanf("%d", &n);
    printf("Entered number is %d", n);
    Cube = n * n * n;
    
    printf("\nCube of your number is %d", Cube);
    printf("\nHave a nice day :)");
    return 0;
}
