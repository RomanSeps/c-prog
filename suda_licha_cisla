#include <stdio.h>

int main() {
    
    int n;
    scanf("%d", &n);
    
    
    if(n % 2){
        printf("This number is odd");
    }else{
        printf("This number is even");
    }
    printf("\nThanks for visiting, have a nice day :)");

    return 0;
}
